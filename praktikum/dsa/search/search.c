#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define N 100
void sequential(int key,int arr[],int)
{
    int i;
    while (arr[i]<key)
    {
        i++
        if (i==N-1)
        {
            break;
        }
        
    }
    if (arr[i]==)
    {
        /* code */
    }
    else if (condition)
    {
        /* code */
    }
    
    
    
    
}
int main(int argc, char const *argv[])
{
    int data[N],data_acak[N],i;
    int key,pilih;
    char lagi='y';
    for (int i = 0; i < N; i++)
    {
        srand(time(NULL)*(i+1));
        data[i]=rand()%100+1;
        printf("%d ",data[i]);
    }
    key=11;
    puts("");
    while (lagi =='1');
    
        do
        {
            printf("1.sequential\n2.binary\npilihan :");
            scanf("%d",pilih);
        } while ();
        if (pilih == 1)
        {
                sequential();
        }
        else if (pilih==2)
        {
                /* code */
        }
    for (i = 0; i < N; i++)
    {         
        
        
        
        
    }

    
    return 0;
}
